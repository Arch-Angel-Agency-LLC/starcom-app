import React from 'react';
import NetRunnerDashboard from '../../NetRunner/NetRunnerDashboard';

/**
 * NetRunner Screen
 * 
 * This component integrates the NetRunner Dashboard into the main UI structure.
 */
const NetRunnerScreen: React.FC = () => {
  return <NetRunnerDashboard />;
};

export default NetRunnerScreen;

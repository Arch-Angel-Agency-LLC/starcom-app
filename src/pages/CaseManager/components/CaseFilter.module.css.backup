.filterContainer {
  background-color: rgba(20, 30, 50, 0.8);
  border-radius: 6px;
  padding: 12px;
  margin-bottom: 15px;
}

.searchForm {
  display: flex;
  align-items: center;
  gap: 8px;
}

.searchInputContainer {
  flex: 1;
  position: relative;
  display: flex;
  align-items: center;
}

.searchIcon {
  position: absolute;
  left: 10px;
  color: #6e7f9e;
}

.searchInput {
  width: 100%;
  padding: 8px 10px 8px 35px;
  border-radius: 4px;
  background-color: rgba(0, 0, 0, 0.2);
  border: 1px solid rgba(100, 130, 200, 0.4);
  color: #e1e6f0;
}

.searchInput:focus {
  outline: none;
  border-color: rgba(100, 130, 200, 0.8);
  box-shadow: 0 0 0 1px rgba(100, 130, 200, 0.4);
}

.clearButton {
  position: absolute;
  right: 10px;
  background: none;
  border: none;
  color: #6e7f9e;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0;
}

.searchButton {
  padding: 8px 15px;
  background-color: rgba(60, 80, 120, 0.8);
  border: 1px solid rgba(100, 130, 200, 0.4);
  border-radius: 4px;
  color: #e1e6f0;
  cursor: pointer;
  transition: all 0.2s ease;
}

.searchButton:hover {
  background-color: rgba(70, 90, 130, 0.9);
}

.expandButton {
  padding: 8px 15px;
  background-color: rgba(30, 40, 60, 0.8);
  border: 1px solid rgba(100, 130, 200, 0.4);
  border-radius: 4px;
  color: #e1e6f0;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 8px;
  transition: all 0.2s ease;
}

.expandButton:hover {
  background-color: rgba(40, 50, 70, 0.9);
}

.expandedFilters {
  margin-top: 15px;
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.filterSection {
  border-top: 1px solid rgba(100, 130, 200, 0.2);
  padding-top: 12px;
}

.filterHeader {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 10px;
}

.filterHeader h3 {
  font-size: 0.9rem;
  color: #e1e6f0;
  margin: 0;
}

.dateRangeControls {
  display: flex;
  gap: 10px;
}

.dateInput {
  display: flex;
  flex-direction: column;
  gap: 5px;
}

.dateInput label {
  font-size: 0.8rem;
  color: #8aa2c8;
}

.dateInput input {
  padding: 6px 10px;
  background-color: rgba(0, 0, 0, 0.2);
  border: 1px solid rgba(100, 130, 200, 0.4);
  border-radius: 4px;
  color: #e1e6f0;
}

.statusTags, .priorityTags, .classificationTags, .tagsList {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}

.tag {
  padding: 5px 10px;
  background-color: rgba(30, 40, 60, 0.8);
  border: 1px solid rgba(100, 130, 200, 0.4);
  border-radius: 4px;
  color: #e1e6f0;
  font-size: 0.8rem;
  cursor: pointer;
  transition: all 0.2s ease;
}

.tag:hover {
  background-color: rgba(40, 50, 70, 0.9);
}

.tagSelected {
  background-color: rgba(60, 100, 160, 0.6);
  border-color: rgba(100, 130, 200, 0.8);
}

.filterActions {
  display: flex;
  justify-content: space-between;
  margin-top: 15px;
}

.clearFiltersButton {
  padding: 8px 15px;
  background-color: rgba(80, 30, 30, 0.8);
  border: 1px solid rgba(200, 100, 100, 0.4);
  border-radius: 4px;
  color: #e1e6f0;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 8px;
  transition: all 0.2s ease;
}

.clearFiltersButton:hover {
  background-color: rgba(100, 40, 40, 0.9);
}

.applyFiltersButton {
  padding: 8px 15px;
  background-color: rgba(30, 80, 40, 0.8);
  border: 1px solid rgba(100, 200, 130, 0.4);
  border-radius: 4px;
  color: #e1e6f0;
  cursor: pointer;
  transition: all 0.2s ease;
}

.applyFiltersButton:hover {
  background-color: rgba(40, 100, 50, 0.9);
}

/* VisualizationModeControls.module.css */
.container {
  margin-bottom: 16px;
  padding: 12px;
  background: rgba(0, 20, 40, 0.9);
  border: 1px solid rgba(64, 224, 255, 0.3);
  border-radius: 8px;
}

.sectionTitle {
  font-size: 12px;
  font-weight: 600;
  color: rgba(64, 224, 255, 0.9);
  margin-bottom: 12px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.primaryModeContainer {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 12px;
}

.primaryButton {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  padding: 8px 12px;
  background: rgba(16, 32, 48, 0.8);
  border: 1px solid rgba(64, 224, 255, 0.2);
  border-radius: 6px;
  color: rgba(255, 255, 255, 0.9);
  font-size: 12px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
  width: 100%;
}

.primaryButton:hover {
  background: rgba(64, 224, 255, 0.1);
  border-color: rgba(64, 224, 255, 0.5);
  transform: translateY(-1px);
}

.primaryButton.active {
  background: rgba(64, 224, 255, 0.2);
  border-color: rgba(64, 224, 255, 0.8);
  color: rgba(64, 224, 255, 1);
  box-shadow: 0 0 8px rgba(64, 224, 255, 0.3);
}

.secondaryModeContainer {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding-left: 8px;
  border-left: 2px solid rgba(64, 224, 255, 0.2);
}

.secondaryButton {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  padding: 6px 10px;
  background: rgba(8, 16, 24, 0.6);
  border: 1px solid rgba(64, 224, 255, 0.15);
  border-radius: 4px;
  color: rgba(255, 255, 255, 0.8);
  font-size: 11px;
  font-weight: 400;
  cursor: pointer;
  transition: all 0.2s ease;
  width: 100%;
}

.secondaryButton:hover {
  background: rgba(64, 224, 255, 0.08);
  border-color: rgba(64, 224, 255, 0.3);
  color: rgba(255, 255, 255, 0.95);
}

.secondaryButton.active {
  background: rgba(64, 224, 255, 0.15);
  border-color: rgba(64, 224, 255, 0.6);
  color: rgba(64, 224, 255, 1);
  box-shadow: 0 0 6px rgba(64, 224, 255, 0.2);
}
